#pragma once

int lerArquivo(int dados[], int loop);
void limpaOutput();
int escreverResultado(long long tempoShell, long long tempoComb, int loop, long long memoriaBytes);
int contarElementos(int loop);
